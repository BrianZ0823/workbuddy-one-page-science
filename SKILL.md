---
name: one-page-science
description: 把任意一篇科普文章/公众号长文/技术博客，按"手账科普"风格重制成可本地预览、可导出 PNG 的多页 HTML 长图。当用户发送一篇文章/链接并希望"出科普页/手账风重制/做成一页纸科普/按模板出图"时使用。This skill should be used when the user provides an article, blog post, or WeChat official-account piece and wants a hand-drawn-journal-style (手账风) popular-science deliverable in HTML + PNG.
license: MIT
agent_created: true
---

# 一页纸科普 · 手账风（one-page-science）

## 1. 目的与适用场景
把一篇普通的科普/文章，提炼成"手账本"风格的轻量科普长图（多页 HTML + 可导出 PNG），用于飞书群/手机端轻量分发、内部培训、老板科普材料。

**触发时机**（命中即启用）：
- 用户发来一篇文章/链接/文件，要求"出科普页""做成一页纸""按手账风重制""科普一下这个"。
- 用户说"按模板01/手账风出图""给我一份可发的科普材料"。

**适用范围**：任意主题的科普/文章（AI 工具、金融、能源、管理方法……均可），不止 AI 类。

## 2. 核心原则
- **提取语法，不提取内容**：保留参考图的设计逻辑（网格、配色、卡片结构、金句框、落款），替换其文案/产品名/原作者水印。详见 `references/style-spec.md`。
- **三档锁定**（每次生成前分清）：
  - **Hard lock**（丢一则不像同一套，不可动）：竖版纸感比例、暖米白底、模块化卡片网格、每卡"1图标+1标题+1行"、底部金句框、最底来源落款、多色低饱和强调轮换。
  - **Soft lock**（可微调）：卡片列数、装饰密度、卡片旋转、图标风格、washi-tape 颜色。
  - **Must replace**（绝不沿用）：原作者水印/署名、原产品名/独有文案、原图独有插画角色。
- **中文两层法**：中文密集文案必须走 HTML/CSS 确定性排版（可预览、零乱码），**不要**用 AI 生图承载中文（必乱码）。本技能默认纯 HTML/CSS 实现手账美学。
- **不编造**：事实/数字/结论来自原文；查不到写"未确认"，不臆造。

## 3. 风格速查（今天最终定稿态）
基础美学见 `references/style-spec.md`；**今天的实测微调为权威最终态**，见 `references/today-refinements.md`。速查：
- 底色暖米白（HTML 实现 `#f7f3e8` 系），纸张纹理噪点 + 左侧螺旋装订（手账本意象）。
- 标题字体 **黑体（SimHei）**；正文 **微软雅黑（Microsoft YaHei, weight 500）** + Noto Sans SC 兜底（幼圆 YouYuan 已弃用：部分字形缺失导致同段混排、违和感强）。
- **禁用圆圈数字**：所有序号用方形/圆角矩形标签或 `1./2.` 文字。
- 模块化 sticker-note 卡片网格（2 列），每卡含：序号标签、图标、中英名、明确安装/操作方法、功能、场景、实操提示词。
- 底部金句高亮框（虚线边框 + 星标）；最底来源/模板落款（可开关，默认保留）。
- 多色低饱和低强调轮换：蓝 `#3b82f6` / 橙 `#ffb366` / 绿 `#8bc34a` / 紫 `#c084fc` / 青 `#5ac8fa` / 黄 `#ffe066`。
- 篇幅按内容定，**不必强行压成一页**；覆盖要全，名称/安装方法/步骤等关键信息不能模糊省略。

## 4. 端到端流程
- **Step1 接收与确认**：识别来源——URL 用 WebFetch 抓全文；粘贴文本直接用；文件用 Read。确认：① 主题（≤12字）② 是否保留来源落款（默认保留，尊重用户即时"去掉来源"指令）。
- **Step2 提炼文案**：把原文结构化为"内容契约"：
  - `topic`：一句话主题（≤12字）
  - `cards[]`：每张 {图标(emoji或SVG), 标题, 一行说明, [安装/操作方法], [功能], [场景], [实操提示词]}
  - `quote`：底部金句（≤20字，可选）
  - `source`：公众号名/作者/链接
  - 提炼而非照搬长段；保留作者+来源；术语转大白话；不编造。
- **Step3 克隆模板产出 HTML**：复制 `assets/base-template.html` → 按内容契约填值。多页按内容扩展（不强行压一页）。套用今天最终风格（黑体标题+雅黑正文、禁圆圈数字、便利贴并排不遮挡）。**首页 `<section class="page">` 务必加 `cover` 类**（`<section class="page cover">`），自动收紧标题/摘要/路径条/速览间距、避免大片留白。每卡必须含明确可操作信息。
- **Step4 本地预览确认**：用 `present_files` 打开 HTML，用户确认风格/文案无误后，才进入分发。
- **Step5 导出 PNG**：运行 `scripts/render_html_to_png.py <html路径>` 逐页渲染 PNG（需 Playwright+chromium；不可用则降级为浏览器打开 HTML 后"打印/截图"导出，脚本已注明）。

## 5. 内容生产规则
- **提炼非照搬**：公众号/文章只做提炼 + 保留作者/来源；长段不整段复制。
- **术语大白话**：技术词给通俗翻译（如 `find-skills→找现成方案`、`RPA→自动操作网页`、`封装→保存成套路`）。
- **每卡结构固定**：1 图标 + 1 标题 + 1 行说明（Hard lock）；详情卡再加"安装/操作方法 + 功能 + 场景 + 实操提示词"。
- **金句框**：底部一句高亮收尾（≤20字）。
- **明确步骤**：涉及"怎么用/怎么装"的条目，必须写出明确可操作的方法（搜「名称」安装 / 走 GitHub / 一键包），不能模糊。

## 6. 质量红线 / 检查清单
- 不像素级抄原图、不 trace 原图独有插画；新图不带原水印。
- 公众号内容提炼 + 保留作者+来源；不编造事实/数字/结论。
- 法律、金融类内容只做辅助定位与风险边界，不替代专业意见；自动化点击/填表需权限+人工确认。
- 飞书/群分发必须人工确认（防误发）。
- 真实模板必须"下载参考图 → 像素采样 → 重写 spec"，绝不再"凭通用美学猜"（历史教训）。
- 中文用 HTML/CSS 确定性排版，不生图承载中文。

## 7. 注意事项速览（详见 `references/pitfalls.md`）
- 看不到图 ≠ 停止：Read 报错就下载原图/换多模态，不要"凭猜"。
- 先确认真实模板形态（单图 vs 长卷）再动手。
- 中文生图必乱码；文字层必须 HTML/CSS。
- 用户偏好：篇幅不压一页、圆圈数字禁用、字号放大留白收紧、来源落款可开关。

## 8. 输出契约
每次交付包含：
1. 最终 HTML（多页手账风，可预览）
2. PNG 图片（逐页或长图，按 Step5）
3. 一句话"保留了什么 / 替换了什么"（如：保留作者署名与来源；将原文案例替换为本期主题）
4. 本地预览路径 + 状态（draft / ready）

## 9. 资源索引
- `assets/base-template.html`：★核心，可克隆的手账风 HTML 模板（CSS 变量 + 占位结构 + 注释）。
- `assets/reference/reference.jpg` / `reference_small.jpg`：真实模板参考图（像素采样源）。
- `references/style-spec.md`：完整风格规格（Hard/Soft lock + 色值 + 可变槽）。
- `references/today-refinements.md`：今天最终风格微调（覆盖项，权威态）。
- `references/pitfalls.md`：注意事项踩坑集（来自实战日志）。
- `scripts/render_html_to_png.py`：HTML → PNG 渲染（Playwright，含降级说明）。
- 成品样例（本工作区，不复制）：`手账风格科普页-WorkBuddy-Skills.html`。
